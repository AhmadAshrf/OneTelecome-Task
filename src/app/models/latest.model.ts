export interface Latest{
    id:string,
    title:string,
    content:string,
    categoryID:string,
    urlToImage:string,
    description:string,
    publishedDate:string, //could be (date) type ^^
    showOnHomepage:string //could be (boolean) type ^^
}